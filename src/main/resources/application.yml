spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/clear_the_fridge?serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: refridge
    password: "0107"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true